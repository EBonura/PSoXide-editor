# Cortex Ignition V1 Hardware Burn Ledger

This ledger is the place where real PS1 CD-R observations become engineering
work. Do not leave a hardware finding only in chat or memory: each entry should
point to a visible checkpoint, a preburn/emulator gate, or a tracked
emulator/engine/SDK fix.

## Rules

1. Record the disc label, source commit, build command, burn speed/media, console
   model, video output, and exact visible result.
2. Preserve any screen color, text code, or hang point verbatim. Use
   `docs/hardware-visual-checkpoints.md` as the code/color map.
3. Convert the finding into one of:
   - a `hardware-boot-visual` checkpoint or text label;
   - a preburn/emulator assertion;
   - an emulator, engine, or SDK fix with a repro command.
4. Do not burn the next candidate while the bringup report has `WARN` or
   `MISSING` rows unless the exception is recorded here.

## Entries

| ID | Date | Disc / build | Hardware observation | Local / emulator evidence | Disposition |
| --- | --- | --- | --- | --- | --- |
| HWB-001 | pre-2026-06-08 | `demo10` / early `cortex_ignition_v1` color-checkpoint discs | Screen colors were used to locate boot progress on real hardware. Exact color/code sequence still needs backfill from notes/photos. | Current visible checkpoint sources: `engine/crates/psx-engine/src/app.rs`, `engine/crates/psx-engine/src/game_app.rs`. | Backfill exact color table, then keep stable names in the visual checkpoint map. |
| HWB-002 | pre-2026-06-08 | `demo10` / text-checkpoint discs | Text display was added after color-only discs to make the stop point identifiable on TV. Exact last visible text still needs backfill. | DuckStation BIOS TTY markers now assert `psx-rt`, editor init, scene init, and CD-DA startup markers. | Add any hardware-only last text code as a DuckStation/Redux expectation or a preburn assertion. |
| HWB-003 | 2026-06-02 evidence, still active on 2026-06-08 | Menu -> gameplay while menu CD-DA remains active | Gameplay showed sky/HUD only in the documented profile path; streamed room data did not become valid content. | `docs/demo10-low-level-hot-paths-2026-06-02.md` records `CD_ROOM_CHUNK_STATUS=5` (`STATUS_CD_ERROR`). `make cortex-ignition-v1-preburn-streaming-guard` now fails until profile rows prove room streaming while CD-DA evidence remains present. | Active blocker. Fix CD-DA/data ownership or route sequencing, then make the stream guard pass before burning. |
| HWB-004 | 2026-06-08 | `cortex_ignition_v1` external emulator matrix | Not a hardware burn. Added to prevent false confidence before the next CD-R. | RetroArch previously emitted `Firmware is missing: scph5501.bin`; the smoke now passes a BIOS-derived `system_directory` and writes a verified PNG screenshot. Mednafen and ares rows are marked `OBSERVED`, not semantic boot success. | Keep emulator comparison rows in the bringup report; promote any emulator disagreement into a focused probe. |
| HWB-005 | 2026-06-09 | `hardware-tests` GPU read-back battery (commit `66cd9af7`, 118 cases) burned at 10x | GPU CHECKS (photos IMG_6132-6135): every drawn TRIANGLE FAILs, every QUAD PASSes, raw VRAM fill PASSes, poly-too-large tri PASSes. 1st fail = flat triangle EXP 0x495AFB4D (emulator) / GOT 0x04121005 (hardware). | Quads are axis-aligned rects -> the 2-triangle split covers them exactly regardless of edge rule, so the divergence is the triangle EDGE-COVERAGE rule (~1px on diagonals), not gross geometry. `emu/crates/emulator-core/src/gpu/raster.rs` is "parity-matched against Redux" (header), i.e. tuned to PCSX-Redux, not silicon. | (1) Vertex explosion: GPU+DMA EXONERATED (a 1px edge diff can't fling a vertex); explosion is pose-specific skinning, reproduce the exact pose. (2) North-star rasterizer fix: needs a follow-up disc that dumps the covered-pixel BITMAP (not a hash) to reverse-engineer the PS1 edge rule, then rewrite raster.rs to match silicon. |
| HWB-006 | 2026-06-09 | `hardware-tests` silicon-rasterizer disc (commit `83424cf5`, GPU cases re-baked) burned at 10x | GPU CHECKS section is **ALL GREEN on real hardware** (photos IMG_6136-6139): all 12 triangle cases that were RED on HWB-005 now PASS -- flat/gouraud tri, edge/neg/wrap/bottom, textured-gouraud (player prim) direct + via OT/DMA + large-span, 8bpp CLUT, deep OT/DMA. ALL HARD FAILURES CLEAR. Global PASS 083 FAIL 009: the 9 fails are 8 known GTE result-read-latency cases (NCLIP MAC0 x4, LZCR x4; emulator fails them too via the latency model) + OP cross-product MAC1 (hardware 0xFFFFFBCE = -1074 vs spec -768; OP unused in cortex, game-irrelevant). MVMVA FC-bug MAC1/2/3 PASS (cv=FC fix confirmed on hw). | Rasterizer rewrite `2f4b0063`: center-sampled Mednafen/DuckStation DDA + determinant-plane interpolation in `gpu.rs` (`for_each_tri_pixel`), Redux scanline rasterizer retired. Verified in-emulator against all 12 recorded silicon hashes before burning (no-burn replay harness in `gpu/tests.rs`). | Triangle rasterization now matches silicon 100%. Resume the vertex-explosion hunt on the silicon-faithful emulator (re-test the player's stretch pose -- the rasterizer was the unmodeled downstream quirk class). OP MAC1 crackable later from 0xFFFFFBCE (pure accuracy). BURN GOTCHA: this drive EJECTS via software but cannot CLOSE -- `drutil tray close` is a no-op, the user must close manually; verify Space Used>0 only after a manual close. |

| HWB-007 | 2026-06-10 | `cortex_explosion_probe` skinned-vertex probe disc (branch commit `da70d905`), fixed idle pose | Overlay photos IMG_6167-6172: AX -2286..-2697 to 1114..1299, BX -1461..-1590 to 1637..1779, LX -2103..-2474 to 507..644, SX -42..-77 to 208..221, PX min == SX min, PX max 258..272. Emulator same pose: AX -250..223, BX -265..226, LX -252..223, SX 136..181, PX 131..181. | The player's view-space X is ~10x wider on hardware at EVERY skinning stage, including AX (the primary-joint MVMVA). LX sits between AX and BX (the lerp is innocent). Numbers are self-consistent: view X -2133 at depth ~3500 projects to ~-37, matching the observed SX -45. PX min equals SX min (the extreme verts are blended ones); PX max ~90px wider than emulator (single-bone verts affected too, milder). | ROOT CAUSE CLASS: the blended path reloads the GTE rotation matrix via CTC2 immediately before every MVMVA (secondary load mid-vertex, primary restore after; the next vertex's primary transform follows the restore). Real silicon needs settle time after CTC2 before a COP2 op reads the control regs; no emulator models it. Explains: blended verts worst (reload per vertex), single-bone milder (reload per part), world clean (reload per long batch), angle-dependence (composed matrix changes which stale elements matter). NEXT: model CTC2 write-settle latency in the emulator (explosion should finally reproduce), fix the engine skinning path (settle gap or restructure), one confirming burn. **SUPERSEDED: the CTC2-settle theory was refuted on silicon by HWB-008's sweep battery; the real mechanism is HWB-010's stale-V0.x.** |

| HWB-008 | 2026-06-10 | Sweep/calibration discs (10x after the burn-speed rule; one 16x retry on phantom burns): 18-NOP settled-engine probe (`240571fb`), RT settle/drop sweep battery, 141-case calibration disc, 155-case big-value/magnitude disc | (a) Settled-engine probe (IMG_6181-6184, airtight disc custody): AX -2347..1226 / BX -1513..1759 / LX -2159..623 -- the 18-NOP gte_write_settle does NOT fix the explosion. (b) RT sweep (IMG_6186-6192): ALL TEN "RT load settle +0..32" and "drop-during-RTPS +0..16" cases PASS -- NO CTC2 write hazard exists in a quiet loop. (c) Calibration (IMG_6193-6196): compose-chain modes x3 PASS (synthetic compose exonerated); MAC0/LZCR settle threshold measured = stale ONLY for the immediately-next instruction. (d) 155-case disc (IMG_6197-6209): scene-NCLIP divergences decode EXACTLY as "cross product with the SY0 terms dropped" (0x874 constant across +1..+48 gaps; five-for-five arithmetic match) -- an SXY write-semantics anomaly, value- and time-independent, still unmodeled. | Emulator MAC0/LZCR model updated to the measured next-instruction-only window with the write-adjacency rule (`a37b1a70`, Crash-safe). CTC2 hazard models left env-gated OFF (historical). 18-NOP engine fix reverted (`d6a45eee`). | CTC2 hazards: refuted, closed. SY0-drop: open, decisive instrument = the 166-case SXY state-dump disc (built + gated, `89891791` + `03ea851b`), awaiting a burn slot. Burn drill upgrades banked: lowest listed speed always; mount + byte-compare PSX.EXE is the only reliable gate (raw read-back slips on this drive). |

| HWB-009 | 2026-06-11 | `cortex_explosion_probe` ALL-STAGES live-capture disc (commit `b116516e`), burned 10x, PSX.EXE gate PASS | Photos IMG_6210-6214, all five pages SC 2810 (one latched stretch event, player visibly shredded): full skinning chain captured in hex -- compose inputs (VI + per-joint model matrices + pose translations), composed matrices as CTC2'd, MVMVA outputs, lerp, projection, FLAG=0 throughout. | Replay through the console-exact GTE core (1100/1100 vs the gte-fuzz console corpus, `ef9e816f`): `cargo run -p emulator-core --release --example gte_skin_replay -- docs/hardware-refs/hwb009-capture-sc2810.txt` -> compose R0/R1 FAIL (X row wholly wrong, Y/Z clean), skin VA/VB FAIL (X only), lerp PASS. Code-verified exclusions (`docs/hardware-refs/hwb010-model-exclusions.py`): stale-MAC1 read, V0-load-delay-whole-op, constant TRX, stale vx, stale row regs all arithmetically impossible. Structural signature: errors are exact additive (D<<12) accumulator terms, D identical per column index across joints (c1 +484, c2 +271). | The explosion is pinned to exact numbers for the first time: the GTE compose output X row diverges from the bit-exact core given identical inputs. Next instrument: HWB-010's TR-readback + dual MAC1 read. |

| HWB-010 | 2026-06-11 | `cortex_explosion_probe` HWB-009-instrumented disc (commit `69ed2336`: `scene::transform_vertex_probed` = byte-identical live schedule + post-op settled MAC1 re-read + TRX/TRY/TRZ readback; overlay page 5), burned 10x, PSX.EXE gate PASS (1,265,664 bytes) | Photos IMG_6215-6219, five pages SC 2147 (fresh stretch event): **page 5 = TR regs read ZERO during every corrupt compose column, immediate-minus-settled MAC1 deltas all ZERO, AT/BT match the loaded T0/T1.** The corrupt MAC1 is stable architectural state; TR is clean; yet the X row is corrupt again (R10 row0 0x0C8D) and the model is exploded on screen. | **MECHANISM DECODED, six exact arithmetic hits across both events with zero free parameters:** the MVMVA computes MAC1 (executed FIRST in the GTE's sequential row pipeline, per the MiSTer core ordering) with the PREVIOUS op's V0.x, while MAC2/MAC3 (executed later) see the fresh value -- the MTC2 VXY0 write commits between the MAC1 and MAC2 compute windows. Predictions vs photos: HWB-010 j9 c1.x `0202`/c2.x `FF57`, HWB-009 j0 c1.x `020A`/c2.x `FF91`, j9 c1.x `01FE`/c2.x `FF90` -- all exact (stale x = previous column's x; z fresh; y indeterminate because VI01=0). This is psx-spx's documented "GTE load delay of 2 instructions", which no public emulator (DuckStation, Redux, Mednafen) or the MiSTer core models. Intermittent in the live game (HWB-010's skin-B op passed while HWB-009's failed): the commit slip is gated by external bus traffic (DMA/IRQ), which is why every quiet-loop synthetic battery passed. Replay: `gte_skin_replay docs/hardware-refs/hwb010-capture-sc2147.txt`. | OPEN: the first column's stale x is deeper than one op back (solved stale values are world-vertex magnitude, not the preceding pose translation) -- next probe records the previous V0. NEXT: (1) env-gated emulator model (MTC2 commit delay + phase-split MVMVA -> explosion reproduces in PSoXide), (2) GTE-keeping engine fix (widen the MTC2-to-op gap; schedule variants self-compared on one disc), (3) one confirming burn (latch SC ~320 = fixed). |

| HWB-011 | 2026-06-11 | `cortex_explosion_probe` FIX disc (engine fix `a2d30030` + emulator model `01786200`), burned 10x, PSX.EXE gate PASS (1,265,664 bytes byte-identical) | **CONFIRMED ON CONSOLE 2026-06-11: the vertex explosion is GONE.** User verdict same day as the burn: "IT WORKS, issue is gone" -- the stretch that survived every prior fix attempt no longer reproduces with the padded schedule. (User notes performance is degraded on this build -- expected: the probe build runs every compose/skin transform TWICE plus the 5-page capture overlay; the shipping cleanup removes the probe scaffolding and promotes the padded schedule to the default.) | Full in-emulator verification matrix under the HWB-010 hazard model (`PSOXIDE_GTE_V0X_STALE=1`): model ON + previous engine = explosion at SC 2810 (the exact HWB-009 silicon score -- first and only in-emulator reproduction); model ON + fix = clean SC 323 with the evidence channel firing (joint deltas 207/440/-177, 399/33/-254, skin AD -104); model OFF = clean SC 323. Fix: `scene::transform_vertex_padded` (2-NOP commit gap between the V0 writes and the MVMVA, distance 2 to 4, GTE-keeping). Core hazard model unit-tested against the HWB-010 silicon photo values. | If SC ~323 with nonzero page-5 D values: the explosion is fixed and attributed in one burn; promote the padded schedule into `transform_vertex_mips` as the default and close the hunt. If SC remains high: page 5 quantifies the residue (deeper commit window or the unpadded RTPS path); widen the pad and re-verify in-emulator first. |

## Next Burn Template

| Field | Value |
| --- | --- |
| Entry ID | HWB-011 |
| Date / time |  |
| Git commit / diff summary |  |
| Disc label |  |
| Build command |  |
| Preburn report | `build/preburn/cortex_ignition_v1/BRINGUP_REPORT.md` |
| Burn media / speed / drive |  |
| Console model / BIOS / modchip |  |
| Video output / display |  |
| Last screen color |  |
| Last text checkpoint |  |
| Audio state | silent / CD-DA / SPU / unknown |
| Pad input route |  |
| Result |  |
| Required code change or gate |  |
