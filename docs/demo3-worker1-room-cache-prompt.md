# Copy-Paste Prompt For Worker 1: Demo3 Room Cache Build Optimisation

Copy everything in this file into Worker 1.

```text
You are Worker 1 on the PSoXide demo3 performance pass.

Your assignment is issue 1 only: move room surface cache build work out of the frame path so demo3 no longer stutters while rooms/chunks are lazily cached during play.

You are not alone in the codebase. Worker 2 is working separately on camera/tile-aware visibility and PVS. Do not revert their edits. Avoid changing Worker 2-owned visibility algorithms unless a tiny call-site adaptation is unavoidable.

Repository:
/home/user/Desktop/repos/PSoXide

Before doing architecture/codebase work, read:
sed -n '1,180p' AGENTS.md
sed -n '1,180p' graphify-out/GRAPH_REPORT.md

Useful graph query:
graphify query "How do editor-playtest room surface caching, generated chunk visibility, and residency/streaming relate in this codebase?"

After modifying code files, run:
graphify update .

Use rg for search. Use apply_patch for manual edits. Do not use destructive git commands. Do not commit or push. Do not revert files you did not intentionally change.

Known dirty files at orchestration time, unrelated to this performance pass:
editor/crates/psxed-project/src/lib.rs
editor/crates/psxed-ui/src/lib.rs

Treat those as user/local work. Do not revert them. If your task genuinely requires touching either file, explain that in your final handoff and keep the edit narrowly scoped.

Baseline benchmark target:
make cook-playtest PROJECT=projects/demo3/project.ron
make build-editor-playtest
cd emu && ./target/release/frontend launch \
  --path ../build/examples/mipsel-sony-psx/release/editor-playtest.exe \
  --guest-frames 60 \
  --steps 120000000 \
  --dump-hw /tmp/psoxide-demo3-worker1-cache-check.ppm \
  --dump-hash \
  --dump-guest-profile

Baseline demo3 hashes:
display_fnv1a_64=0x3c4e18c498e13da0
vram_fnv1a_64=0x7725d623d3394114
screenshot=/tmp/psoxide-demo3-baseline-restored-60.png

Baseline demo3 profile:
render total       3,746,709 cycles/frame
room               2,989,336 cycles/frame
player               568,672 cycles/frame
world flush/sort     108,828 cycles/frame
update               117,579 cycles/frame
room cache build     280,105 cycles/frame average, 1,956,017 max hit

tri prims            1,294/frame
world commands        1,294/frame
room cells              405/frame
room cells drawn        238/frame
room cells culled       167/frame
room surfaces           745/frame
room chunks               4/frame
room cache builds        12 total during 60-frame capture

Current memory map findings:
PS1 RAM total: 2 MiB
EXE payload: 845,824 B / 826 KiB / 40.3%
.text:       190,456 B / 186 KiB /  9.1%
.data:       655,360 B / 640 KiB / 31.3%
.bss:        363,040 B / 354 KiB / 17.3%
heap+stack runway: about 803 KiB / 39.2%

Important memory smell:
CACHED_ROOM_SURFACES is 0x28000 = 160 KiB in .data because its EMPTY value is non-zero.
OT is 0x2000 = 8 KiB in .data.
The current runtime mutable room cache duplicates data already represented in cooked room/world assets.

Actual VRAM pressure is currently low:
Framebuffer: about 300 KiB
Room textures: about 4.8 KiB actual uploaded
Model atlas: about 16.5 KiB actual uploaded
Font + shadow: about 6 KiB

Architecture summary:
Demo3 is a geometry-heavy playtest level cooked into engine/examples/editor-playtest/generated/.

Current playtest path:
1. Editor/cooker creates room chunks and generated Rust manifest.
2. Runtime includes room/world/model/texture bytes through include_bytes!.
3. Runtime picks active chunk rooms near the player/camera.
4. Runtime builds per-room surface caches lazily into fixed global arrays.
5. Runtime uses generated visibility cell lists plus runtime camera/frustum tests to draw cached room surfaces.
6. World commands are sorted/flushed into the PS1 ordering table.

Your problem:
The lazy runtime cache build is causing stutter. The existing cache representation is useful, but the cache should be generated by the cooker or loaded once through compact static/cooked slices. Demo3 should not spend frame time decoding/precomputing room cache data.

Primary goal:
Runtime should not call the expensive room-cache builder during normal demo3 rendering. Cached room cell/vertex/surface data should come from generated/cooked records or equivalent static slices.

Current bad baseline:
room cache build total: 16,806,315 cycles in 60-frame run
room cache build per_frame: 280,105
room cache build max_hit: 1,956,017
room cache builds: 12

Target:
room cache build should be zero or near-zero during the 60-frame render profile.
No large single-frame cache-build spike.
Default demo3 visual hash should ideally remain unchanged.

Your write ownership:
engine/crates/psx-engine/src/world_render.rs
engine/crates/psx-level/src/lib.rs
editor/crates/psxed-project/src/playtest/schema.rs
editor/crates/psxed-project/src/playtest/manifest.rs
editor/crates/psxed-project/src/playtest.rs
engine/examples/editor-playtest/src/main.rs

In engine/examples/editor-playtest/src/main.rs, limit yourself to cache build/load functions and draw-cache plumbing:
ActiveRoomSurfaceCache
ROOM_SURFACE_CACHE_BY_ROOM
ROOM_SURFACE_CACHE_*_CURSOR
active_room_surface_cache_for
cache_active_room_surfaces
build_active_room if necessary
the render loop only where it reads cache slices

Do not redesign these Worker 2 areas:
fill_precomputed_visible_cells
chunk_activation_score
best_spatial_chunk_candidate
active room scoring / PVS selection

Key files and landmarks:
engine/examples/editor-playtest/src/main.rs
  load_active_room_window
  refresh_active_room_window_if_needed
  cache_active_room_surfaces
  active_room_surface_cache_for
  build_active_room
  draw loop around draw_indexed_cached_room_vertex_lit_visible_cells

engine/crates/psx-engine/src/world_render.rs
  cache_room_vertex_lit_surfaces
  draw_indexed_cached_room_vertex_lit_visible_cells
  CachedRoomCell
  CachedRoomSurface

engine/crates/psx-level/src/lib.rs
  LevelChunkRecord
  LevelRoomVisibilityRecord
  LevelVisibilityCellRecord
  add new cache records here if needed

editor/crates/psxed-project/src/playtest/manifest.rs
  emits generated manifest tables

Detailed current runtime cache state:
The runtime has global mutable cache arenas:
CACHED_ROOM_CELLS
CACHED_ROOM_VERTICES
CACHED_ROOM_PROJECTED_VERTICES
CACHED_ROOM_PROJECTED_VALID
CACHED_ROOM_PROJECTED_DEPTHS
CACHED_ROOM_SURFACES
ROOM_SURFACE_CACHE_BY_ROOM
ROOM_SURFACE_CACHE_CELL_CURSOR
ROOM_SURFACE_CACHE_VERTEX_CURSOR
ROOM_SURFACE_CACHE_SURFACE_CURSOR

When a room/chunk is needed, runtime calls active_room_surface_cache_for, which eventually calls cache_room_vertex_lit_surfaces to decode/precompute:
CachedRoomCell
WorldVertex
CachedRoomSurface

Short-term acceptable result:
Generated static room-cache records are used directly by runtime rendering.
The mutable global cache arrays are removed or bypassed for cooked rooms.
Runtime still keeps per-frame projected vertex/depth/valid scratch because projection is camera-dependent.

Long-term compatible direction:
Room cache payloads should be streamable chunk assets later.
Do not hard-code an architecture that requires every room cache to stay resident forever.
Represent the cache as per-room/per-chunk slices so a future CD stream pack can page only nearby chunks.

Suggested design:
1. Add generated room-cache tables to the playtest manifest, for example:
   pub static ROOM_CACHE_CELLS: &[LevelCachedRoomCellRecord] = &[...];
   pub static ROOM_CACHE_VERTICES: &[LevelCachedRoomVertexRecord] = &[...];
   pub static ROOM_CACHE_SURFACES: &[LevelCachedRoomSurfaceRecord] = &[...];
   pub static ROOM_SURFACE_CACHES: &[LevelRoomSurfaceCacheRecord] = &[...];

2. Add runtime-facing record structs to engine/crates/psx-level/src/lib.rs.
   Keep them compact and no-alloc. Use integer fields matching the existing cached structures. Avoid heap containers.

3. Add conversion helpers in engine/crates/psx-engine/src/world_render.rs, or expose safe const fn constructors for:
   CachedRoomCell
   CachedRoomSurface
   WorldVertex

4. Prefer drawing from generated/static slices without copying into mutable global cache arrays.

5. Change runtime ActiveRoomSurfaceCache to refer to generated static slices or generated record ranges, rather than ranges inside global mutable cache arenas.

6. Keep fallback support only if needed for placeholder/uncooked builds. If no cooked cache is present, fallback to the old runtime cache is acceptable. Demo3 should not hit fallback.

7. Reduce or remove mutable cache arenas. At minimum, demo3 should no longer use:
   CACHED_ROOM_CELLS
   CACHED_ROOM_VERTICES
   CACHED_ROOM_SURFACES
   ROOM_SURFACE_CACHE_*_CURSOR

8. Keep camera-dependent scratch:
   CACHED_ROOM_PROJECTED_VERTICES
   CACHED_ROOM_PROJECTED_VALID
   CACHED_ROOM_PROJECTED_DEPTHS

Important correctness details:
Generated cache reconstruction must preserve:
center
baked RGB
kind
ordinal
wall direction
split / triangle index
UVs
material slot
vertex indices

Tests to add:
1. Cooker emits one room cache record per generated room/chunk.
2. Generated cache cell/vertex/surface counts match old cache_room_vertex_lit_surfaces output for a known test room.
3. Generated cached sample reconstruction preserves center, baked RGB, kind, ordinal, wall direction, split, triangle index, UVs, material slot, and vertex indices.
4. Runtime lookup of generated cache for a room does not call the fallback builder for cooked demo-style rooms.

Useful test areas:
editor/crates/psxed-project/src/playtest.rs
engine/crates/psx-engine/src/world_render.rs tests

Run at least:
cargo test --manifest-path engine/crates/psx-engine/Cargo.toml world_render render3d
cargo test --manifest-path editor/Cargo.toml -p psxed-project playtest
make cook-playtest PROJECT=projects/demo3/project.ron
make build-editor-playtest
cd emu && ./target/release/frontend launch \
  --path ../build/examples/mipsel-sony-psx/release/editor-playtest.exe \
  --guest-frames 60 \
  --steps 120000000 \
  --dump-hw /tmp/psoxide-demo3-worker1-cache-check.ppm \
  --dump-hash \
  --dump-guest-profile

Performance report required:
display hash
vram hash
screenshot path
render total cycles/frame
room cycles/frame
room cache build cycles/frame
room cache builds counter
max_hit for room cache build if still present
tri prims/frame
room surfaces/frame
room cells drawn/frame

Expected best case:
room cache build disappears from active render profile
default visual hash unchanged
stutter max frame reduced because cache build is not happening after launch

Do not optimize visibility in this worker. If room render remains around 3M cycles but stutters are gone, that is a successful Worker 1 result.

Final handoff format:
Worker: 1
Goal:

Changed files:
- ...

Implementation summary:
- ...

Tests run:
- ...

Demo3 default:
- display hash:
- vram hash:
- screenshot:
- render total cycles/frame:
- room cycles/frame:
- room cache build cycles/frame:
- tri prims/frame:
- room cells drawn/frame:
- room surfaces/frame:

Known risks:
- ...

Merge notes:
- ...
```
