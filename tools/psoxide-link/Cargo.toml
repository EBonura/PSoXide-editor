[package]
name         = "psoxide-link"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "Hydrate a pinned PSoXide source tree into a game repository, so its Cargo path dependencies and linker script resolve to one known SDK."

[dependencies]
serde_json = { workspace = true }
