// Generated by `psxed_project::playtest::write_package` —
// do not edit by hand. Regenerate with the editor's
// `Cook & Play` action or the equivalent CLI command.
//
// This is the **placeholder** version committed so the
// example compiles before any cook has run. It carries the
// type definitions but zero rooms / zero entities.

#[derive(Debug, Clone, Copy)]
pub struct PlaytestRoomRecord {
    pub name: &'static str,
    pub world_bytes: &'static [u8],
    pub origin_x: i32,
    pub origin_z: i32,
    pub sector_size: i32,
}

#[derive(Debug, Clone, Copy)]
pub struct PlaytestSpawnRecord {
    pub room: u16,
    pub x: i32,
    pub y: i32,
    pub z: i32,
    pub yaw: i16,
    pub flags: u16,
}

#[derive(Debug, Clone, Copy)]
pub enum PlaytestEntityKind {
    Marker,
    StaticMesh,
}

#[derive(Debug, Clone, Copy)]
pub struct PlaytestEntityRecord {
    pub room: u16,
    pub kind: PlaytestEntityKind,
    pub x: i32,
    pub y: i32,
    pub z: i32,
    pub yaw: i16,
    pub resource_slot: u16,
    pub flags: u16,
}


pub static ROOMS: &[PlaytestRoomRecord] = &[
];

pub static PLAYER_SPAWN: PlaytestSpawnRecord = PlaytestSpawnRecord { room: 0, x: 0, y: 0, z: 0, yaw: 0, flags: 0 };

pub static ENTITIES: &[PlaytestEntityRecord] = &[
];
