# `engine/` — PSX runtime engine

The engine layer that sits on top of the [SDK](../sdk): a Scene/App
framework plus the shared level manifest schema. Host-independent and
MIPS-target only — this is what authored games and the editor-playtest
runtime are built against.

`engine/Cargo.toml` is its own workspace; each example under
[`examples/`](examples) is additionally its own workspace so it can carry
PSX target flags independently.

## Crates

| Crate | Purpose |
|-------|---------|
| [`psx-engine`](crates/psx-engine) | Scene/App framework + canonical unit types (`Angle`, `Frames`) on top of the SDK. |
| [`psx-level`](crates/psx-level) | Shared `no_std` level manifest schema: assets, materials, rooms, residency. Generated by the editor, consumed by the runtime. |

## Examples

| Example | Shows |
|---------|-------|
| [`hello-engine`](examples/hello-engine) | Minimal Scene/App skeleton. |
| [`showcase-3d`](examples/showcase-3d) | 3D transforms and rendering. |
| [`showcase-fog`](examples/showcase-fog) | Depth fog. |
| [`showcase-lights`](examples/showcase-lights) | Lighting. |
| [`showcase-model`](examples/showcase-model) | Cooked model rendering. |
| [`showcase-particles`](examples/showcase-particles) | Particle effects. |
| [`showcase-text`](examples/showcase-text) | Font/text rendering. |
| [`game-pong`](examples/game-pong), [`game-breakout`](examples/game-breakout), [`game-invaders`](examples/game-invaders), [`game-magikaaaaaarp-pong`](examples/game-magikaaaaaarp-pong) | Small complete games. |
| [`hardware-tests`](examples/hardware-tests) | On-target hardware behaviour checks. |
| [`editor-playtest`](examples/editor-playtest) | Renders a level cooked from the editor on real hardware. Has its own [README](examples/editor-playtest/README.md). |

## See also

- [Root README](../README.md) — example gallery with screenshots.
- [`docs/engine-rearchitecture.md`](../docs/engine-rearchitecture.md) — engine design direction.
- [`docs/world-grid-architecture.md`](../docs/world-grid-architecture.md), [`docs/level-residency.md`](../docs/level-residency.md) — room/level runtime model.
