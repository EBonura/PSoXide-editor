# Collection launcher geometry

This core-only crate owns the existing integer title-ring, reflection and decorative geometry shared by the demo disc and Arcade. It has no MMIO, allocator, renderer or game lifecycle dependency. Constants retain the established collection UI policy. Rendering, assets and input remain caller-owned.
