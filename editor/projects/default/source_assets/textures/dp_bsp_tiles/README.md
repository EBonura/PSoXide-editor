# DP BSP tile pipeline

See [`../../../DP_BSP_TEXTURES.md`](../../../DP_BSP_TEXTURES.md) for the visual
roles, output paths, and rebuild instructions.

The original pack is not copied wholesale into the repository. The checked-in
selection contains only the 16 source cells used by the first BSP kit, their
two 64 x 64 enlargement variants, provenance hashes, and the supplied licence.
