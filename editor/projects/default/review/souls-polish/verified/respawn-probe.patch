--- /Users/ebonura/Desktop/repos/PSoXide-cortex-polish/engine/examples/editor-playtest/src/playtest_scene.rs
+++ /tmp/cortex-polish/build-src/engine/examples/editor-playtest/src/playtest_scene.rs
@@ -980,6 +980,19 @@
         self.refresh_actor_pose_snapshots(ctx);
         self.resolve_enemy_melee(ctx);
         self.resolve_player_melee(ctx);
+        // DIAGNOSTIC ONLY: force death with a live shot; exercise real respawn.
+        static mut DEATH_INJECTED: bool = false;
+        if unsafe { !DEATH_INJECTED } && self.combat_projectiles.len() > 0 {
+            unsafe { DEATH_INJECTED = true; }
+            let config = self.player_stance_config;
+            for _ in 0..2 {
+                let active = self.player_stance.active();
+                self.player_stance.apply_damage(&mut self.player_vitality, active, u16::MAX, &config);
+            }
+            telemetry::debug_log("probe death:live-projectile-broken-stance");
+            self.arm_player_death(true, 60, self.gameplay_tick(ctx.sim_tick), ctx.video_hz);
+        }
+
     }
 
     fn render(&mut self, ctx: &mut Ctx) {
--- /Users/ebonura/Desktop/repos/PSoXide-cortex-polish/engine/examples/editor-playtest/src/playtest_runtime.rs
+++ /tmp/cortex-polish/build-src/engine/examples/editor-playtest/src/playtest_runtime.rs
@@ -664,6 +664,14 @@
         };
         self.motor.snap_to(position, motor_yaw);
         self.reset_life_combat();
+        if self.combat_projectiles.len() == 0 && self.player_stance.can_swap()
+            && !self.player_stance.is_broken(VitalityChannelId::One)
+            && !self.player_stance.is_broken(VitalityChannelId::Two)
+            && self.player_vitality.pool(VitalityChannelId::One).current() == PLAYER_MAX_HEALTH
+            && self.player_vitality.pool(VitalityChannelId::Two).current() == PLAYER_MAX_HEALTH {
+            telemetry::debug_log("probe respawn:clean-full-health");
+        } else { telemetry::debug_log("probe respawn:FAILED"); }
+
         self.hazard_death_ticks_remaining = 0;
         self.anim_state = PlayerAnim::Idle;
         self.anim_blend_from = None;
--- /Users/ebonura/Desktop/repos/PSoXide-cortex-polish/engine/examples/editor-playtest/src/game_logic_runtime.rs
+++ /tmp/cortex-polish/build-src/engine/examples/editor-playtest/src/game_logic_runtime.rs
@@ -546,6 +546,7 @@
                     };
                     if self.combat_projectiles.spawn(spawn).is_ok() {
                         telemetry::debug_log("enemy projectile:release");
+                        if attack.entity() == 2 { telemetry::debug_log("probe charged-enemy:release"); }
                         self.queue_gameplay_sfx(LevelGameplaySfxEvent::ProjectileLaunch);
                         let _ = self.game_entities.commit_deferred_projectile(attack, emitter);
                         released |= 1u16 << emitter;
